# Torny CRM API base URL. Same origin as the public directory API.
# Copy this file to `.env` (gitignored) to override locally.
VITE_API_BASE_URL=https://ieg3lhlyy0.execute-api.ap-southeast-2.amazonaws.com/Prod
